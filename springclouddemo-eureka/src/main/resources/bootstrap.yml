spring:
  application:
    name: @pom.artifactId@
  profiles:
    active: dev
  # 配置中心
#  cloud:
#    config:
#      fail-fast: true
#      name: ${spring.application.name}
#      profile: ${spring.profiles.active}
#      discovery:
#        enabled: true
#        service-id: springclouddemo-config